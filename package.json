{
  "name": "education-helper-bot",
  "version": "1.0.0",
  "description": "Telegram Web App + bot для выдачи учебных материалов с оплатой через ЮMoney",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:css": "node build-css.js",
    "watch:css": "node build-css.js && echo 'To watch CSS changes, install concurrently and use: concurrently \"npm run watch:colors\" \"npm start\"'"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "node-fetch": "^2.6.7",
    "node-telegram-bot-api": "^0.61.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "autoprefixer": "^10.5.4",
    "postcss": "^8.5.28",
    "tailwindcss": "^4.3.3"
  }
}
